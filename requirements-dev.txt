# Dev/test-only dependencies. Install with:  pip install -r requirements-dev.txt
# Runtime deps live in requirements.txt.

pytest>=8.0
ruff>=0.6        # lint gate; config in pyproject.toml ([tool.ruff]). Run: ruff check .
